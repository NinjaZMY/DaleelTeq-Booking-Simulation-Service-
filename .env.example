# This file means that this project requires an .env file following this template
# Copy this file to .env and fill in the values with your actual credentials
# DO NOT commit the .env file to version control (it's listed in .gitignore)

# Database Credentials Template
DB_USERNAME=booking_user
DB_PASSWORD=changeme
DB_URL=jdbc:postgresql://localhost:5432/booking_db
DB_DRIVER=org.postgresql.Driver

# Application Properties
APP_NAME=DaleelTeq Booking Service
APP_PORT=8080
APP_PROFILE=dev

